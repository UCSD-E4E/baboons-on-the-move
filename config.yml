preprocess:
    kernel_size: 7

motion_detector:
    history_frames: 3

    registration:
        good_match_percent: 0.3
        ransac_max_error: 5
        ssc_num_ret_points: 1000
        ssc_tolerence: 0.1

    quantize_frames:
        scale_factor: 18

    dbscan:
        eps: 2
        min_samples: 8
        kernel: 3